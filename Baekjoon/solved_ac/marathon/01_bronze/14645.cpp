#include <iostream>

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int N, K;
  cin >> N >> K;

  while (N--) {
    int A, B;
    cin >> A >> B;
  }

  cout << "비와이";

  return 0;
}